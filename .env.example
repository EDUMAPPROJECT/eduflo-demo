# ===========================================
# Firebase (전화번호 인증용)
# ===========================================
# Firebase Console (https://console.firebase.google.com/) 에서
# 프로젝트 선택 → 프로젝트 설정(⚙️) → 일반 → "내 앱"에서 웹 앱 추가 후 나오는 설정값을 복사하세요.
#
# VITE_FIREBASE_API_KEY=AIza...
# VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
# VITE_FIREBASE_PROJECT_ID=your-project-id
# VITE_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
# VITE_FIREBASE_MESSAGING_SENDER_ID=123456789
# VITE_FIREBASE_APP_ID=1:123456789:web:abc123...
#
# 위 6개 변수를 .env 파일에 그대로 붙여넣고, 값만 본인 프로젝트 값으로 바꾸면 됩니다.

# ===========================================
# 백엔드 (Supabase Edge Functions)
# ===========================================
# Lovable에서 Supabase를 쓰는 경우 VITE_SUPABASE_URL만 있으면 됩니다.
# firebase-signup, firebase-login 호출 시 VITE_BACKEND_URL이 없으면
# VITE_SUPABASE_URL을 사용해 /functions/v1/... 을 호출합니다.
# Edge Function 배포 방법은 DEPLOYMENT.md "Supabase Edge Functions 배포 (Lovable 사용 시)" 참고.
# (별도 백엔드가 있으면 아래를 설정)
# VITE_BACKEND_URL=https://your-project.supabase.co
#
# Edge Function 배포 후 Supabase 대시보드 → Project Settings → Edge Functions 에서
# 다음 Secret을 설정해야 합니다 (firebase-signup, firebase-login 사용 시):
#   - FIREBASE_API_KEY: Firebase 웹 API 키 (위 VITE_FIREBASE_API_KEY 와 동일한 값)
#   - SUPABASE_URL: Supabase 프로젝트 URL
#   - SUPABASE_SERVICE_ROLE_KEY: 서비스 롤 키 (사용자 생성 등에 사용)
